const ErrorMessage = () => {
  return (
    <div>
      <p>
        Uh oh! It seems there was an error loading the images. Please refresh
        the page and try again.
      </p>
    </div>
  );
};

export default ErrorMessage;
